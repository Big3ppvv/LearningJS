function makeQueueWithLoop(elements, num){
    if(elements.length === 0) return [num];
    const proccesedQueue = [];
    elements.forEach( (element, index) => {
        if(index === 0) return
        
    });
}
   
const array = [5,6,7];
const num = 9;
const result = makeQueueWithLoop(array, num);
console.log(result);
